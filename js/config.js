window.CHART_SMART_CONFIG = Object.freeze({
  // Configure a same-origin backend route such as "/api/voice/coach".
  // Provider credentials belong on that server and must never be added here.
  coachTtsEndpoint: "",
  enableBrowserSpeechTest: false
});
